# ===========================================
# Local Development Setup
# ===========================================
# 1. Copy this file to .env: cp .env.example .env
# 2. Set up your own MongoDB instance (local or MongoDB Atlas free tier)
# 3. Update the values below with your configuration
# 4. See README for database seeding instructions
# ===========================================

# MongoDB - Use your own MongoDB instance
# Local MongoDB: mongodb://localhost:27017
# MongoDB Atlas: mongodb+srv://username:password@cluster.mongodb.net
MONGO_URI=mongodb://localhost:27017
DB_NAME=hangman_game

# JWT Configuration
JWT_SECRET=your-secret-key-here-change-this-to-something-secure
JWT_EXPIRY=24h

# Server Configuration
NODE_ENV=development
PORT=3000

# CORS - Allowed origins (comma-separated, no spaces)
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000

# Deployment URLs (optional - only needed for production/staging)
PROD_URL=
STAGING_URL=
CUSTOM_URL=
